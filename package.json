{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && mpm start"
  },
  "author": "Shaked_BA",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
